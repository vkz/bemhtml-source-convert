block serp-item {
    elem title, tag: 'h2'

    elem title-link, default: {
        var ctx = this.ctx;
        delete ctx.elem;
        applyCtx(this.extend(ctx, {
            block: 'link',
            mix: { block: this.block, elem: this.elem }
        }));
    }
}
