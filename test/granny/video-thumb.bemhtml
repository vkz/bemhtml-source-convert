block video-thumb {

    attrs: {
        style: 'background-image: url(' + this.ctx.bgImg + ')'
    }

    content: [
        this.ctx.content,
        { elem: 'play' },
        {
            elem: 'duration',
            content: this.ctx.duration
        }
    ]

    elem play, tag: 'i'
}
